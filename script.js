// script.js
function showLogin() {
    // ... (same as before)
}

function showSignup() {
    // ... (same as before)
}

function signup() {
    // ... (same as before)
}

function login() {
    // ... (same as before)
}

function logout() {
    // ... (same as before)
}

function aiChat() {
    const input = document.getElementById("ai-input").value;
    const response = processAI(input); // Call the AI function
    document.getElementById("ai-response").textContent = response;
}

function updatePreference(key, value) {
    // ... (same as before)
}

function loadPreferences(username) {
    // ... (same as before)
}

function randomNumber() {
    // ... (same as before)
}

function updateTime() {
    // ... (same as before)
}

function displayCustomMessage(){
    // ... (same as before)
}
